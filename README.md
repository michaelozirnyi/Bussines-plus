#Business plus

##command for install bower
```  bower install ```

##command for install sass
``` gem install sass ```

###command for install watch
``` gem install compass ```

###compile scss command
``` compass watch ```